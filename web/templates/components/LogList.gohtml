{{ define "components/LogList.gohtml" }}
  <table class="table">
    <thead>
      <tr>
        <th>Message</th>
        <th>Happened</th>
        <th>Date</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{ range .data }}
        <tr>
          <td>{{ .Error }}</td>
          <td><small>{{ EpochToTimeAgoHappened .Time }} ago</small></td>
          <td><small>{{ EpochToHumanReadable .Time }}</small></td>
          <td><a href="{{ $.baseURL }}/logs/{{ .UID }}">View</a></td>
        </tr>
      {{ end }}
    </tbody>
  </table>
  <section>
    <a href="{{ $.baseURL }}/logs?offset={{ SubtractTillZero $.offset 50 }}"
      >← prev</a
    >
    <a href="{{ $.baseURL }}/logs?offset={{ Add $.offset 50 }}">next →</a>
  </section>
{{ end }}
